/*#include <bits/stdc++.h>

using namespace std;

int32_t main(const int argc, const char** argv, const char** env) {
	std::ios::sync_with_stdio(false);
	std::cin.tie(nullptr);

	size_t loop, a, b, buffer;

	std::cin >> loop;
	for ( size_t i = NULL; i < loop; ++i ) {
		buffer = 1;
		std::cin >> a >> b;
		for ( size_t j = NULL; j < b; j++ ) { buffer = (buffer * a) % 10; }
		if ( buffer == NULL ) {
			std::cout << 10 << std::endl;
		} else {
			std::cout << buffer << std::endl;
		}
	}
	return EXIT_SUCCESS;
}*/